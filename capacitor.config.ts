import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todoload.app',
  appName: 'todoload',
  webDir: 'dist/frontend',
  bundledWebRuntime: false,
};

export default config;
